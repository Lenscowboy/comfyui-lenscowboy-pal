[project]
name = "comfyui-lenscowboy-pal"
description = "3D previsualisation and spatial layout node for ComfyUI by LensCowboy. Embeds the PAL viewport inside ComfyUI for scene layout, camera setup, and multipass render output (beauty, depth, normals)."
version = "1.0.0"
license = { file = "LICENSE" }
requires-python = ">=3.9"
dependencies = []

[project.urls]
Repository = "https://github.com/Lenscowboy/comfyui-lenscowboy-pal"

[tool.comfy]
PublisherId = "lenscowboy"
DisplayName = "PAL Layout (LensCowboy)"
Icon = ""
